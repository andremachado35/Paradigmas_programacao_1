
/**
 * Write a description of class ExtraEquipa here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class ExtraEquipa extends Equipa {
	// instance variables - replace the example below with your own
	private HashMap<Integer, FichaMais> extra;

	/**
	 * Constructor for objects of class ExtraEquipa
	 */
	public ExtraEquipa(String nom) {
		super(nom);
	}
	
	public ExtraEquipa(String nom, Collection<? extends FichaJogador> colfbase,
	                   Collection<? extends FichaMais> colfextra) {
	     super(nom, colfbase);
	     

	/**
	 * An example of a method - replace this comment with your own
	 * 
	 * @param  y   a sample parameter for a method
	 * @return     the sum of x and y 
	 */
	public int sampleMethod(int y)
	{
		// put your code here
		return x + y;
	}
}
