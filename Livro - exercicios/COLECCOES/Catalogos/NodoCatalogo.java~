
/**
 * Write a description of class NodoCatalogo here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
  public class NodoCatalogo extends AbsCatalogo {
	// instance variables - replace the example below with your own
	private String info;
	public NodoCatalogo(String info) { this.info = info; }
	public NodoCatalogo clone() { return new NodoCatalogo(info); }
}
